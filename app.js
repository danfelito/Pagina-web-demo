(()=>{'use strict';async function boot(){try{const text=await fetch('/app.js.b64',{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('No se pudo cargar el paquete');return r.text()});const raw=atob(text.trim());const bytes=Uint8Array.from(raw,c=>c.charCodeAt(0));if(!('DecompressionStream'in window))throw new Error('Este navegador no admite descompresión moderna. Usa Chrome o Edge actualizado.');const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));const source=await new Response(stream).text();(0,eval)(source)}catch(e){const el=document.getElementById('app');if(el)el.innerHTML='<div class="fatal">'+String(e.message||e)+'</div>';console.error(e)}}boot()})();
